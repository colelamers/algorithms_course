#include <iostream>
#include <vector>
#include <algorithm>
#include"MTFElement.h"
using namespace std;

#ifndef MTF_H_
#define MTF_H_

class MTF {

public:

    static MTFElement encode(vector<char> &str) {
        return MTFElement(); // placeholder to ensure project compiles
    }

    static vector<char> decode(MTFElement &mtf) {
        return vector<char>(); // placeholder to ensure project compiles
    }
};

#endif /* MTF_H_ */
