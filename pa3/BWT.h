#include <iostream>
#include <unordered_map>
#include <map>
#include <vector>
#include <algorithm>
#include "SuffixArray.h"
using namespace std;

#ifndef BWT_H_
#define BWT_H_

class BWT {
    
public:

    static vector<char> encode(vector<char> &str) {
        return vector<char>(); // placeholder to ensure project compiles
    }

    static vector<char> decode(vector<char> &bwt) {
        return vector<char>(); // placeholder to ensure project compiles
    }
};

#endif /* BWT_H_ */
